<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">

    <property name="logback.log.dir" value="/app/logs/tsp-data-access-provider-common" />
    <property name="logback.model.name" value="tsp-data-access-provider-common" />
    <conversionRule conversionWord="customcolor" converterClass="com.ecarx.LogBackColorful"/>
    <!--输出到控制台 ConsoleAppender-->
    <appender name="consoleLog" class="ch.qos.logback.core.ConsoleAppender">
        <!--展示格式 layout-->
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>
                <!--格式化输出：%d:表示日期  %thread:表示线程名  %-5level:级别从左显示5个字符宽度
                %logger{36}——日志输出者的名字 %msg:日志消息 %n:是换行符-->
                <pattern>%d{HH:mm:ss.SSS} [%thread] %customcolor(%-5level) %cyan(%logger{50}) - %msg%n</pattern>
            </pattern>
        </layout>
    </appender>

    <!--输出到日志文件-->
    <appender name="fileInfoLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--日志名称，如果没有File属性，那么只会使用FileNamePattern的文件路径规则 -->
        <!--<File>${logback.log.dir}/info.${logback.model.name}.log</File>-->
        <!--滚动策略，按照时间滚动 TimeBasedRollingPolicy-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--文件路径,定义了日志的切分方式——把每一天的日志归档到一个文件中,以防止日志填满整个磁盘空间-->
            <FileNamePattern>${logback.log.dir}/info.${logback.model.name}.%d{yyyy-MM-dd}.%i.log</FileNamePattern>
            <MaxFileSize>50MB</MaxFileSize>
            <!--只保留最近30天的日志-->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <!--日志输出编码格式化-->
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d [%thread] %-5level %logger{36} %line - %msg%n</pattern>
        </encoder>
    </appender>

    <!--指定日志输出级别-->
    <root level="DEBUG">
        <appender-ref ref="consoleLog"/>
    </root>
    <root level="INFO">
        <appender-ref ref="fileInfoLog"/>
    </root>

</configuration>